apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "emp-dir-helm.fullname" . }}-test-connection"
  labels:
    {{- include "emp-dir-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "emp-dir-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
